%bcond_without tests
%bcond_without weak_deps

%global __os_install_post %(echo '%{__os_install_post}' | sed -e 's!/usr/lib[^[:space:]]*/brp-python-bytecompile[[:space:]].*$!!g')
%global __provides_exclude_from ^/opt/ros/kilted/.*$
%global __requires_exclude_from ^/opt/ros/kilted/.*$

%global __cmake_in_source_build 1

Name:           ros-kilted-gtsam2mrpt-serial
Version:        0.3.0
Release:        1%{?dist}%{?release_suffix}
Summary:        ROS gtsam2mrpt_serial package

License:        MIT
URL:            https://github.com/MRPT/gtsam2mrpt_serial
Source0:        %{name}-%{version}.tar.gz

Requires:       ros-kilted-gtsam
Requires:       ros-kilted-mola-common
Requires:       ros-kilted-mrpt-containers
Requires:       ros-kilted-mrpt-io
Requires:       ros-kilted-mrpt-math
Requires:       ros-kilted-mrpt-poses
Requires:       ros-kilted-mrpt-random
Requires:       ros-kilted-mrpt-rtti
Requires:       ros-kilted-mrpt-serialization
Requires:       ros-kilted-mrpt-system
Requires:       ros-kilted-ros-workspace
BuildRequires:  cmake3
BuildRequires:  ros-kilted-gtsam
BuildRequires:  ros-kilted-mola-common
BuildRequires:  ros-kilted-mrpt-containers
BuildRequires:  ros-kilted-mrpt-io
BuildRequires:  ros-kilted-mrpt-math
BuildRequires:  ros-kilted-mrpt-poses
BuildRequires:  ros-kilted-mrpt-random
BuildRequires:  ros-kilted-mrpt-rtti
BuildRequires:  ros-kilted-mrpt-serialization
BuildRequires:  ros-kilted-mrpt-system
BuildRequires:  ros-kilted-ros-workspace
Provides:       %{name}-devel = %{version}-%{release}
Provides:       %{name}-doc = %{version}-%{release}
Provides:       %{name}-runtime = %{version}-%{release}

%description
A C++ library offering a GTSAM ⇆ mrpt-serialization bridge.

%prep
%autosetup -p1

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree and source it.  It will set things like
# CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/kilted/setup.sh" ]; then . "/opt/ros/kilted/setup.sh"; fi
mkdir -p .obj-%{_target_platform} && cd .obj-%{_target_platform}
%cmake3 \
    -UINCLUDE_INSTALL_DIR \
    -ULIB_INSTALL_DIR \
    -USYSCONF_INSTALL_DIR \
    -USHARE_INSTALL_PREFIX \
    -ULIB_SUFFIX \
    -DCMAKE_INSTALL_PREFIX="/opt/ros/kilted" \
    -DCMAKE_PREFIX_PATH="/opt/ros/kilted" \
    -DSETUPTOOLS_DEB_LAYOUT=OFF \
%if !0%{?with_tests}
    -DBUILD_TESTING=OFF \
%endif
    ..

%make_build

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree and source it.  It will set things like
# CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/kilted/setup.sh" ]; then . "/opt/ros/kilted/setup.sh"; fi
%make_install -C .obj-%{_target_platform}

%if 0%{?with_tests}
%check
# Look for a Makefile target with a name indicating that it runs tests
TEST_TARGET=$(%__make -qp -C .obj-%{_target_platform} | sed "s/^\(test\|check\):.*/\\1/;t f;d;:f;q0")
if [ -n "$TEST_TARGET" ]; then
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree and source it.  It will set things like
# CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/kilted/setup.sh" ]; then . "/opt/ros/kilted/setup.sh"; fi
CTEST_OUTPUT_ON_FAILURE=1 \
    %make_build -C .obj-%{_target_platform} $TEST_TARGET || echo "RPM TESTS FAILED"
else echo "RPM TESTS SKIPPED"; fi
%endif

%files
/opt/ros/kilted

%changelog
* Wed Sep 23 2026 Jose-Luis Blanco-Claraco <joseluisblancoc@gmail.com> - 0.3.0-1
- Autogenerated by Bloom

* Sat Feb 21 2026 Jose-Luis Blanco-Claraco <joseluisblancoc@gmail.com> - 0.2.0-1
- Autogenerated by Bloom

